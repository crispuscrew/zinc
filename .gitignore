# Go
bin/
*.test
*.out
coverage.*
# stray binaries from `go build ./...` in a module dir (named after the dir); the real
# builds go to bin/ via the container
/launcher/gui/gui
/launcher/tui/tui
/menu/example/dmenu/dmenu

# Editors
.obsidian/
.idea/
.vscode/

# OS
.DS_Store

# Agent worktrees (isolated per-agent checkouts; the work lands on their own branches)
.claude/worktrees/
