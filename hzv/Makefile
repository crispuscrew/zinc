# HyprZinc Virtualization (hzv). Build logic is shared with hzp/hzl via ../tool.mk
# ("the same logic, only different paths"). Run from this directory: `make <target>`.

TOOL := hzv

include ../tool.mk
