# common - the shared schema + validation library (pure stdlib, no binary). It includes
# only the containerized checks (check.mk); the tool build targets (tool.mk) do not
# apply, since common produces no binary. Run from this directory: `make check`.

include ../check.mk
