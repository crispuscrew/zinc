# launcher/common - the UI-agnostic core shared by the Zinc launchers (the app store, the
# zcr delegate, the fuzzy matcher). Like `common` it is a library (no binary), so it
# includes only the containerized checks (check.mk); the tool build targets (tool.mk) do
# not apply. Run from this directory: `make check`.

include ../../check.mk
