# zlt - Zinc's launcher (TUI): a fast fuzzy picker over the defined apps that shells out
# to the zcr binary to run them. Build logic is shared with zcc/zcr via ../../tool.mk.
# Run from this directory: `make <target>`.

TOOL := zlt

include ../../tool.mk
