# core — HyprZinc's shared functional core (config / runspec / store / launch),
# consumed by hzc and hzl. It is a LIBRARY module: no binary, so it uses only the
# containerized checks from check.mk. Run from here: `make check`, `make vendor`.
include ../../check.mk
