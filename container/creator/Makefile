# zcc — Zinc's app-definition tool: it authors app files (~/.config/zinc/apps) and
# shells out to the zcr binary to run them. The build logic is shared with zcr via
# ../../tool.mk ("the same logic, only different paths"). Run from this directory:
# `make <target>`.

TOOL := zcc

include ../../tool.mk
