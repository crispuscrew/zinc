// Command hzl is the HyprZinc Launcher — the keyboard-first launcher and smart
// executor (Super+G). This is a build skeleton: it exists so all three tools
// share the same module layout and reproducible container build. The gioui UI
// arrives in milestone M7 (see ../ROADMAP.md).
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stdout, "hzl (HyprZinc Launcher): not implemented yet — see ROADMAP M7")
}
